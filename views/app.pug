html(lang=lang)
    head 
        title= translation["index.pageTitle"]||"index.pageTitle"
        script(src="index.js", type="module")
        link(rel="stylesheet", href="style.css") 
    body.theme-light
        = `NAME: ${app.name}; ID: ${app.id}` 